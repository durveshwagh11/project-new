# This file is generated by Nx.
# Build the docker image with `npx nx docker:build @durvesh-project/durvesh-project`.
# Tip: Modify "docker:build" options in project.json to change docker build args.
#
# Run the container with `nx docker:run @durvesh-project/durvesh-project -p 3000:3000`.
#
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

RUN corepack enable


WORKDIR /app

COPY dist .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.

RUN pnpm install

CMD [ "node", "main.js" ]
